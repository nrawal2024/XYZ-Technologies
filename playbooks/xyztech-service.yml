apiVersion: v1
kind: Service
metadata:
  name: xyztech-service
spec:
  type: NodePort
  selector:
    app: xyztech-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
